[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Redirect API calls to backend (update URL after deploying backend)
[[redirects]]
  from = "/api/*"
  to = "https://cyber-sentinelai.onrender.com"
  status = 200
  force = true
